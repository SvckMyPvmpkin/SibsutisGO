services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=sibsutisgo'
      - 'POSTGRES_PASSWORD=admin'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432'
  redis:
    image: 'redis:latest'
    ports:
      - '6379'
